# Vim
*~
*.sw[p_]

# Sublime Text
*.sublime-project
*.sublime-workspace

# Ruby Gem
*.gem
.bundle
Gemfile.lock
**/vendor/bundle

# Python (tools/build-brand-assets.py)
__pycache__/

# Generator originals of the hero banner, current and superseded; ~8 MB each,
# kept locally, never served.
assets/img/banner.png
assets/img/banner-old.png

# Node.js and NPM
node_modules
npm-debug.log*
package-lock.json
codekit-config.json

# macOS
.DS_Store

# Jekyll generated files
.jekyll-cache
.jekyll-metadata
.sass-cache
_asset_bundler_cache
_site

# Local AI assistant notes (kept out of the published site and the repo)
CLAUDE.md
